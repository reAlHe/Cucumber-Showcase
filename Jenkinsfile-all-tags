pipeline {
    agent any
    environment {
        FLAG_FILE = '/var/jenkins_home/shared/scm_change_detected.flag'
        DISPLAY = ':99'
    }
    triggers {
        cron('*/5 * * * *') // Runs at 2 AM every night
    }
    stages {
        stage('Nightly Build') {
            steps {
                script {
                        echo 'Running all tests'
                        // sh './gradlew clean test'
                        sh './gradlew clean'
                }
            }
        }
    }
    post {
        always {
            script {
                if (fileExists(FLAG_FILE)) {
                    deleteFile FLAG_FILE
                }
            }
        }
    }
}